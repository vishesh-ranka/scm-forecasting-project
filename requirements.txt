# Pipeline
duckdb>=1.5
lightgbm>=4.7
hierarchicalforecast>=1.5
scipy>=1.13
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
matplotlib>=3.9
# scikit-learn is not used for modelling; on macOS it supplies the libomp copy
# that src/train_base_forecasts.py points LightGBM at.
scikit-learn>=1.5

# Dashboard
streamlit>=1.40
plotly>=5.22
# PNG chart export. kaleido v1 also needs a Chrome binary: `plotly_get_chrome`
kaleido>=1.0
